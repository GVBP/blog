import { protractor, promise, browser, element, by, ElementFinder, ExpectedConditions } from "protractor"
import { ContatoLinks } from "../../contato/page-objects/ContatoLinks";

/**
 * Page Object com métodos relacionados a tela de Home referentes a verificação de links.
 * 
 * @export
 * @class HomeLinks
 */
export class HomeLinks extends ContatoLinks {}